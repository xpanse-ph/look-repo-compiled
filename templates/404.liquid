{% comment %} <div class="page-width">
  <div class="empty-page-content text-center">
    <h1>{{ 'general.404.title' | t }}</h1>
    <p>{{ 'general.404.subtext' | t }}</p>
    <p>
      <a href="{{ routes.root_url }}" class="btn btn--has-icon-after">{{ 'general.404.link' | t }}{% include 'icon-arrow-right' %}</a>
    </p>
  </div>
</div> {% endcomment %}

<div class="container mx-auto">
  <div class="block p-8 text-left my-12">
    <h1 class="text-mdxlg font-bold mb-8 tracking-wide">{{ 'general.404.title' | t }}</h1>
    <p class="text-base tracking-wider mb-8">{{ 'general.404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="text-smbase underline hover:no-underline transition-all duration-300 ease-in-out">{{ 'general.404.link' | t }}</a>
  </div>
</div>