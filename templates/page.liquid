{%if page.handle == 'look-loyalty' %}
	{% section 'page-lookloyalty' %}
{%else%}
  <div class="page-width">
    <div class="grid">
      <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
        <div class="text-center section-header">
          <h1>{{ page.title }}</h1>
        </div>

        <div class="rte">
          {{ page.content }}
        </div>
      </div>
    </div>
  </div>
{%endif%}